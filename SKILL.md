---
name: contest-forge
description: 공모전·해커톤을 요강 분석 → 아이디어 선별 → MVP → 발표자료까지 한 파이프라인으로 진행한다. 아이디어 공모전·해커톤·창업지원사업·AI Nativeness 트랙을 다룬다. `/contest-forge`로 직접 호출한다.
argument-hint: "[slug 또는 새 공모전 이름]"
disable-model-invocation: true
---

<!--
disable-model-invocation: 이 스킬은 홈 디렉터리에 폴더를 만들고 여러 단계에 걸친 긴
파이프라인을 시작한다. "발표자료 만들어줘" 같은 일반 요청에 자동 발동하면 곤란하므로
사용자가 /contest-forge로 명시 호출할 때만 뜨게 한다.

주의: argument-hint와 disable-model-invocation은 Claude Code 전용 필드다. claude.ai
업로드나 Skills API로 패키징하면 허용 필드(name/description/license/compatibility/
metadata/allowed-tools) 밖이라 하드 에러가 난다. 그 경로로 배포하려면 두 줄을 지운다.
-->

<!-- 형식 규약: 이 스킬 안의 `§n` 참조는 항상 같은 파일 안의 절을 가리킨다.
     다른 파일을 가리킬 때는 반드시 `3-mvp.md §4`처럼 파일명을 붙인다. -->


# contest-forge — 공모전 파이프라인

공모전 하나를 `~/contests/<slug>/` 폴더 하나로 관리한다. 단계별 산출물이 다음 단계의 입력이 되며, **사람이 반드시 판단해야 하는 지점 2곳에서 멈춘다.**

---

## 0. 진입 라우팅

**인자 없이 호출된 경우**
1. `~/contests/INDEX.md`를 Read. 없으면 폴더와 함께 새로 만든다.
2. 진행 중 공모전을 **마감 임박순**으로 제시하고 무엇을 이어갈지 묻는다.

```
진행중 3건 — 마감 임박순
 1) contest-a   D-16  ②ideate  → 아이디어 선택 대기
 2) contest-b     D-52  ③mvp     → 데모 구현
 3) contest-c       —     ①recon   → 요강 입력 필요
 N) 새 공모전 등록
```

**인자가 기존 slug인 경우** → `~/contests/<slug>/STATE.md`를 읽고 `current_stage`부터 재개.

**인자가 새 이름/URL인 경우** → slug를 정하고 폴더를 만든 뒤 ①recon 진입.

> URL만 받았다면 **자동 수집하지 않는다.** 국내 공모전 사이트는 SPA·HWP 첨부·로그인 벽이 흔해 실패율이 높다. 곧바로 요강 전문을 요청한다. (§3 참조)

---

## 1. 디렉토리 규약

```
~/contests/
  INDEX.md                  전체 대시보드 (마감·단계·다음 할 일)
  <slug>/
    STATE.md                현재 단계·결정 기록·게이트 통과 여부
    brief.md      ①         구조화된 요강
    rubric.md     ①         심사표 (기계 참조용)
    research.md   ②         3축 리서치 + 출처 URL
    ideas.md      ②         12개 발산 + 채점 + 상위 3개 견적서
    plan.md       ③         선택안 풀기획안 (15섹션)
    mvp/          ③         demo.html 또는 실제 레포
    submission.md ④         제출 규격에 맞춘 본문
    deck.html     ④         16:9 발표 슬라이드
    script.md     ④         발표 대본 + 예상질문
```

`INDEX.md` 형식:

```markdown
# 공모전 현황

| slug | 공모전명 | 마감 | D- | 유형 | 단계 | 다음 할 일 |
|---|---|---|---|---|---|---|
| contest-b | <공모전 이름> | 2026-09-30 | 52 | hackathon | ③mvp | 데모 시나리오 2 구현 |
```

D-day는 `brief.md`의 마감일과 오늘 날짜로 계산한다. 마감을 모르면 `—`.

---

## 2. 단계와 게이트

| 단계 | 지시문 | 산출 | 게이트 |
|---|---|---|---|
| ① recon | `references/1-recon.md` | brief.md, rubric.md | 🚧 참가 자격·필수항목 미확인 시 ②로 못 감. 자격 미달 확인 시 즉시 중단 |
| ② ideate | `references/2-ideate.md` | research.md, ideas.md | 🚧 사용자가 1개 고를 때까지 정지 |
| ③ mvp | `references/3-mvp.md` | plan.md, mvp/ | — |
| ④ report | `references/4-report.md` | submission.md, deck.html, script.md | 제출 규격 검사 |

**해당 단계에 진입할 때 그 reference 파일만 Read한다.** 미리 다 읽지 않는다.

### ai-nativeness 유형의 경로

현장에서 과제를 받는 형식이라 발상할 것이 없다. 다만 **①을 통째로 건너뛰지는 않는다** — 건너뛰면 유형을 판정할 주체도, STATE.md를 만들 곳도 없어진다.

| 단계 | ai-nativeness에서 |
|---|---|
| ① recon | **경량 recon만 수행** — 대회명·일정·트랙·심사방식만 채운다. 주제·제출규격은 현장에서 나오므로 `[현장 확정]`으로 둔다. **요강 필수항목 게이트는 면제.** STATE.md·INDEX.md 행은 정상 생성 |
| ② ideate | **건너뜀** |
| ③ mvp | 현장에서 과제 문장을 받으면 `/contest-forge <slug>`로 재진입 → `references/3-mvp.md` §4 ai-nativeness 모드 |
| ④ report | **사용하지 않음.** ③ 종료 시 `current_stage: 완료` |

게이트는 임의로 넘기지 않는다. 사용자가 "그냥 진행해"라고 하면 넘기되, 무엇을 모른 채 진행하는지 한 줄로 명시하고 STATE.md에 기록한다.

---

## 3. 요강 확보 — 자동 수집하지 않는다

검증 결과 WebFetch로는 절반 이상이 실패한다(정부기관 게시판은 본문이 비고 HWP 첨부에만 상세가 있으며, 민간 공모전 사이트는 JS 렌더 SPA인 경우가 많다). 그래서 **사용자 입력이 기본값**이다.

요청 문구:

> 요강 전문을 붙여넣거나 파일 경로를 주세요. PDF·이미지·txt·md는 제가 직접 읽습니다.

- `.hwp` 파일은 **읽을 수 없다.** 파싱을 시도하지 말고 "한글에서 PDF로 저장한 뒤 다시 주세요" 또는 "본문을 붙여넣어 주세요"라고 안내한다.
- 사용자가 URL을 고집하면 한 번은 WebFetch를 시도해도 된다. 실패하면 두 번 시도하지 말고 즉시 붙여넣기를 요청한다.
- 여러 소스(웹 본문 + 첨부 + FAQ)를 받으면 모두 합쳐 brief.md 하나로 정규화한다.

---

## 4. 관통 규칙

이 규칙들은 모든 단계에 적용된다.

### 4.1 루브릭이 지배한다
`rubric.md`가 아이디어 채점, MVP 범위, 발표 목차를 전부 결정한다. 어느 단계에서든 "이게 어느 심사항목을 먹는가"를 답할 수 없는 작업은 하지 않는다.

### 4.2 수치는 출처 필수
- `plan.md`·`deck.html`·`submission.md`의 모든 수치·통계는 `research.md`의 출처 URL과 연결한다.
- 못 찾은 수치는 **지어내지 않고** `[출처 필요]`로 남긴다.
- 산출 직후 미해결 `[출처 필요]` 목록을 사용자에게 별도 보고한다.
- **성과지표는 숫자가 아니라 측정방법으로 쓴다.**
  - ✗ "공격 차단율 87%"
  - ✓ "공격 차단율: 준비된 권한 초과·프롬프트 인젝션 공격 중 차단된 비율"

### 4.3 실현가능성 = 대회 기간 내 데모 구현 가능성
팀 역량 프로필을 받지 않으므로, '실현가능성' 점수는 팀 스펙이 아니라 **대회 기간·제출 형식 안에서 동작하는 데모를 만들 수 있는가**로 채점한다. 외부 승인·실계약·모델 신규 학습이 필요한 안은 감점한다.

### 4.4 실데이터 금지
모든 MVP는 합성 데이터와 모의 API를 쓴다. 실제 개인정보·실거래·실계좌를 쓰지 않는다. MVP 문서에는 **제외·축소 범위를 반드시 명시**한다 — 과대약속은 질의응답에서 무너진다.

### 4.5 문서 스타일
- 한국어. 기획안·제출문서는 서술체, 개조식 혼용은 요강 지정을 따른다.
- **이모지 금지.** 데이터 저널리즘 미학 — 절제된 타이포, 표와 구조로 말한다.
- `deck.html`은 외부 CSS/JS/폰트 링크 없는 자체완결형 단일 파일.

---

## 5. 유형 판별 → 루브릭 프리셋

①recon에서 요강을 읽고 유형을 정한다. 애매하면 사용자에게 확인한다.

| 유형 | 판별 신호 | 프리셋 | MVP 산출물 |
|---|---|---|---|
| `idea-contest` | 제출물이 문서·PPT뿐, 구현 요구 없음 | `assets/rubrics/idea-contest.md` | demo.html 단일 목업 |
| `hackathon` | 기간 내 개발, 작동하는 결과물 제출 | `assets/rubrics/hackathon.md` | 실제 실행 가능 레포 |
| `startup-grant` | 사업계획서·시장성·자금 지원 | `assets/rubrics/startup-grant.md` | 목업 + 사업성 자료 |
| `ai-nativeness` | AI 활용 역량 자체를 채점, 현장 과제, 코드·커밋·로그 심사 | `assets/rubrics/ai-nativeness.md` | 레포 + 증거 5축 |

요강에 심사기준이 **명시되어 있으면 그것이 우선**이다. 프리셋은 빈칸만 메우고, 채운 항목은 `출처=프리셋(추정)`으로 표시한다.

---

## 6. STATE.md

`assets/STATE.template.md`를 복사해 만든다. 매 단계 종료 시 갱신하고, INDEX.md의 해당 행도 같이 고친다.

**둘이 어긋나면 STATE.md가 진실이다.** INDEX.md는 각 STATE.md에서 재생성할 수 있는 파생 뷰로 취급한다.

세션이 바뀌어도 STATE.md만 읽으면 이어서 작업할 수 있어야 한다. 중요한 결정(왜 이 아이디어를 골랐는지, 무엇을 제외했는지)은 반드시 STATE.md에 남긴다.

---

## 7. 구현 위임

`③mvp`에서 **실제 레포**를 만들 때는 직접 짜지 말고 위임한다.

1. `mvp/HANDOFF.md`에 스펙·수용기준·검증 명령어·금지사항을 쓴다.
2. `Agent(model: "opus")`로 위임한다. 독립 모듈이 여럿이면 병렬로 띄운다.
3. 메인 루프는 **시나리오 e2e가 실제로 재현되는지만** 검수한다.

단일 HTML 목업은 위임하지 않고 직접 쓴다 — 디자인 일관성이 더 중요하다.

---

## 8. 방어 규칙

- 요강에 없는 제출 규격을 추측해서 만들지 않는다. 모르면 묻는다.
- 사용자가 고르지 않은 아이디어로 진행하지 않는다.
- 채점표의 점수는 **모델의 자기추정**이다. 항상 그렇게 표시하고, 상위 3개에는 반드시 반론을 붙인다.
- 마감이 임박(D-3 이내)한데 ③④가 안 끝났으면, 새 기능 대신 **제출 가능한 최소 형태로 수렴**할 것을 먼저 제안한다.
